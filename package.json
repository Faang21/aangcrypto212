{
  "name": "aangcrypto212",
  "version": "1.0.0",
  "description": "Pixel crypto game with NFT skills, houses, vending machine and private chat",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.19.0",
    "wrangler": "^4.71.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.3"
  }
}
